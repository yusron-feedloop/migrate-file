/// <reference types="node" />
import { ICounter } from './base.count';
export declare class PptxCounter extends ICounter {
    private static _parseXmlFile;
    static count(buffer: Uint8Array | Buffer): Promise<number>;
}
